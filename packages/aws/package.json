{
  "name": "@packages/aws",
  "version": "0.1.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "prettier --check .",
    "fmt:fix": "prettier --write .",
    "test": "vitest run",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "@aws-sdk/client-cost-explorer": "3.922.0"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
